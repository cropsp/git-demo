так так. що ми тут маємо, це не пороожній док, ура

щось взагалі не бачу тексту, why i don' see the text in this file ?

def get_name(message): #получаем фамилию
    global name;
    name = message.text;
    bot.send_message(message.from_user.id, 'Какая у тебя фамилия?');
    bot.register_next_step_handler(message, get_surnme);
--------------------------------------------------------

сьогодні вже 22,05,2022 продовжуємо навчання

/додамо ще тексту

новенький текст в гылцы мейн